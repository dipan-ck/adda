services:
    server:
        container_name: adda-server
        build:
            context: .
        ports:
            - "8008:8008"
            - "42000-42100:42000-42100/udp" # ⭐ Mediasoup RTC ports
            - "42000-42100:42000-42100/tcp" # ⭐ Mediasoup RTC ports
        env_file:
            - .env
        restart: unless-stopped

# docker compose up --build
